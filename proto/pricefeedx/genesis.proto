syntax = "proto3";
package pricefeedx;

import "gogoproto/gogo.proto";
import "pricefeedx/params.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/bandprotocol/oracle-consumer/x/pricefeedx/types";

// GenesisState defines the pricefeedx module's genesis state.
message GenesisState {
  Params params = 1 [ (gogoproto.nullable) = false ];
  string port_id = 2;
  // this line is used by starport scaffolding # genesis/proto/state
}
